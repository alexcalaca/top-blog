<h1 align="center"><%= "Category: " + @category.name %></h1>

<div align="center">
  <%= will_paginate @category_articles, :previous_label => 'Previous', :next_label => 'Next' %>
</div>
<%= render 'articles/article', obj: @category_articles %>

<div align="center">
  <%= will_paginate @category_articles, :previous_label => 'Previous', :next_label => 'Next' %>
</div>